# 📋 Riceflow - Rice Quality Monitoring System
## เอกสารข้อกำหนดความต้องการของระบบ (Software Requirements Specification)

**Version:** 2.0  
**Last Updated:** 2025-11-30  
**Project Name:** Riceflow  
**Platform:** Web Application (Mobile-First) + Native Mobile Apps (iOS/Android)

---

## 📖 สารบัญ

1. [ภาพรวมโครงการ](#1-ภาพรวมโครงการ)
2. [เทคโนโลยีที่ใช้](#2-เทคโนโลยีที่ใช้)
3. [ระบบการจัดการผู้ใช้และสิทธิ์](#3-ระบบการจัดการผู้ใช้และสิทธิ์)
4. [โมดูลหลักของระบบ](#4-โมดูลหลักของระบบ)
5. [โครงสร้างหน้าและเส้นทาง](#5-โครงสร้างหน้าและเส้นทาง)
6. [ฐานข้อมูล](#6-ฐานข้อมูล)
7. [การแจ้งเตือน](#7-การแจ้งเตือน)
8. [การรักษาความปลอดภัย](#8-การรักษาความปลอดภัย)
9. [ข้อกำหนดที่ไม่ใช่ฟังก์ชัน](#9-ข้อกำหนดที่ไม่ใช่ฟังก์ชัน)

---

## 1. ภาพรวมโครงการ

### 1.1 วัตถุประสงค์
Riceflow เป็นระบบติดตามและวิเคราะห์คุณภาพข้าวแบบ Real-time ที่เชื่อมต่อกับอุปกรณ์ IoT สำหรับวัดค่าคุณภาพข้าวต่างๆ โดยมีเป้าหมาย:
- ติดตามค่าคุณภาพข้าวจากอุปกรณ์หลายเครื่องพร้อมกัน
- แสดงผลข้อมูลในรูปแบบกราฟและตารางที่เข้าใจง่าย
- แจ้งเตือนเมื่อค่าวัดเกินเกณฑ์ที่กำหนด
- รองรับการใช้งานบนมือถือและเว็บบราวเซอร์

### 1.2 กลุ่มผู้ใช้เป้าหมาย
| กลุ่มผู้ใช้ | คำอธิบาย |
|------------|----------|
| **Guest** | ผู้เยี่ยมชมที่ไม่ได้เข้าสู่ระบบ ดูข้อมูลเครื่องที่เปิดเป็นสาธารณะได้ |
| **User** | ผู้ใช้ทั่วไปที่ลงทะเบียนแล้ว ดูข้อมูลเครื่องที่ได้รับสิทธิ์ |
| **Admin** | ผู้ดูแลระบบ จัดการผู้ใช้ อุปกรณ์ และข่าวสาร |
| **Superadmin** | ผู้ดูแลระบบสูงสุด มีสิทธิ์จัดการทุกอย่างรวมถึงการกำหนดสิทธิ์เข้าถึงอุปกรณ์ |

### 1.3 ขอบเขตของระบบ
```
┌─────────────────────────────────────────────────────────────────┐
│                        RICEFLOW SYSTEM                          │
├─────────────────────────────────────────────────────────────────┤
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │  IoT Devices │  │   Web App    │  │  Mobile App  │          │
│  │  (เครื่องวัด)  │  │   (React)    │  │ (Capacitor)  │          │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘          │
│         │                 │                 │                   │
│         └─────────────────┼─────────────────┘                   │
│                           │                                     │
│              ┌────────────▼────────────┐                       │
│              │     Supabase Backend    │                       │
│              │  ┌──────────────────┐   │                       │
│              │  │  PostgreSQL DB   │   │                       │
│              │  │  Edge Functions  │   │                       │
│              │  │  Realtime        │   │                       │
│              │  │  Authentication  │   │                       │
│              │  └──────────────────┘   │                       │
│              └─────────────────────────┘                       │
│                           │                                     │
│              ┌────────────▼────────────┐                       │
│              │   Firebase (FCM)        │                       │
│              │   Push Notifications    │                       │
│              └─────────────────────────┘                       │
└─────────────────────────────────────────────────────────────────┘
```

---

## 2. เทคโนโลยีที่ใช้

### 2.1 Frontend Stack
| เทคโนโลยี | เวอร์ชัน | วัตถุประสงค์ |
|----------|---------|-------------|
| React | 18.x | UI Framework |
| TypeScript | 5.x | Type Safety |
| Vite | 5.x | Build Tool |
| Tailwind CSS | 3.x | Styling |
| shadcn/ui | - | UI Component Library |
| Radix UI | - | Accessible Primitives |
| React Query | 5.x | Server State Management |
| React Router | 6.x | Routing |
| Recharts | 2.x | Data Visualization |
| Capacitor | 7.x | Native Mobile Apps |

### 2.2 Backend Stack
| เทคโนโลยี | วัตถุประสงค์ |
|----------|-------------|
| Supabase | Backend as a Service |
| PostgreSQL | Primary Database |
| Supabase Auth | Authentication |
| Supabase Realtime | Real-time Data Sync |
| Edge Functions | Serverless Functions |
| Firebase FCM | Push Notifications |

---

## 3. ระบบการจัดการผู้ใช้และสิทธิ์

### 3.1 ประเภทบทบาท (Roles)
```typescript
type AppRole = "user" | "admin" | "superadmin";
```

### 3.2 สิทธิ์การเข้าถึงตามบทบาท

| ฟีเจอร์ | Guest | User | Admin | Superadmin |
|--------|-------|------|-------|------------|
| ดูหน้าแรก | ✅ | ✅ | ✅ | ✅ |
| ดูอุปกรณ์สาธารณะ | ✅ | ✅ | ✅ | ✅ |
| ดูอุปกรณ์ส่วนตัว | ❌ | ✅ | ✅ | ✅ |
| ดูกราฟ/ข้อมูลวัด | ✅* | ✅ | ✅ | ✅ |
| ตั้งค่าการแจ้งเตือน | ❌ | ✅ | ✅ | ✅ |
| ดูประวัติแจ้งเตือน | ❌ | ✅ | ✅ | ✅ |
| จัดการผู้ใช้ | ❌ | ❌ | ✅ | ✅ |
| จัดการอุปกรณ์ | ❌ | ❌ | ✅ | ✅ |
| จัดการข่าวสาร | ❌ | ❌ | ✅ | ✅ |
| กำหนดสิทธิ์เข้าถึงอุปกรณ์ | ❌ | ❌ | ❌ | ✅ |

*Guest สามารถดูได้เฉพาะอุปกรณ์ที่เปิดเป็นสาธารณะ

### 3.3 การ Authentication
- **Email/Password Login**: รองรับการเข้าสู่ระบบด้วยอีเมลและรหัสผ่าน
- **Session Management**: ใช้ Supabase Auth สำหรับจัดการ session
- **Protected Routes**: เส้นทางที่ต้องการสิทธิ์เฉพาะจะถูกป้องกันด้วย `ProtectedRoute` component
- **Waiting List**: ผู้ใช้ใหม่อาจต้องรอการอนุมัติจาก Admin

---

## 4. โมดูลหลักของระบบ

### 4.1 โมดูลอุปกรณ์ (Equipment Module)

#### 4.1.1 รายการอุปกรณ์
- แสดงรายการอุปกรณ์ทั้งหมดที่ผู้ใช้มีสิทธิ์เข้าถึง
- แสดงสถานะออนไลน์/ออฟไลน์ของอุปกรณ์
- แสดงชื่อ, ตำแหน่ง, และเวลาอัปเดตล่าสุด
- รองรับการค้นหาและกรอง

#### 4.1.2 รายละเอียดอุปกรณ์ (Device Details)
- แสดงข้อมูลอุปกรณ์โดยละเอียด
- แสดงค่าวัดล่าสุดของแต่ละพารามิเตอร์
- ลิงก์ไปยังกราฟและประวัติ

#### 4.1.3 ประวัติการวัด (Measurement History)
- แสดงประวัติค่าวัดในรูปแบบตาราง
- เลือกช่วงเวลา: 1 ชั่วโมง, 24 ชั่วโมง, 7 วัน, 30 วัน
- แสดงค่าเฉลี่ย, ค่าต่ำสุด, ค่าสูงสุด
- Export ข้อมูลได้

### 4.2 โมดูลกราฟ (Graph Module)

#### 4.2.1 Graph Monitor
- แสดงกราฟแบบ Real-time
- เลือกเมตริกที่ต้องการแสดงได้หลายตัว
- รองรับหลายรูปแบบกราฟ:
  - Classic
  - Gradient
  - Neon
  - Area
  - Line

#### 4.2.2 Graph Summary
- สรุปข้อมูลในรูปแบบกราฟ
- เปรียบเทียบค่าวัดหลายพารามิเตอร์
- รองรับการบันทึก Preset

#### 4.2.3 Preset Management
- บันทึกการตั้งค่ากราฟที่ใช้บ่อย
- เรียกใช้ Preset ที่บันทึกไว้
- แชร์ Preset ได้

### 4.3 โมดูลการวัดคุณภาพ (Quality Measurements)

#### 4.3.1 พารามิเตอร์การวัด
ระบบรองรับการวัดค่าคุณภาพข้าวหลายพารามิเตอร์:

| พารามิเตอร์ | Symbol | หน่วย | คำอธิบาย |
|------------|--------|-------|----------|
| Head Rice | head_rice | % | เปอร์เซ็นต์ข้าวเต็มเมล็ด |
| Whole Kernels | whole_kernels | % | ข้าวเต็มเมล็ด |
| Total Brokens | total_brokens | % | ข้าวหัก |
| Small Brokens | small_brokens | % | ข้าวหักเล็ก |
| Whiteness | whiteness | - | ความขาว |
| Chalkiness | heavy_chalkiness_rate | % | ความขุ่น |
| Yellow Rice | yellow_rice_rate | % | ข้าวเหลือง |
| Paddy Rate | paddy_rate | % | ข้าวเปลือก |
| Red Line Rate | red_line_rate | % | ข้าวแดง |
| และอื่นๆ | ... | ... | ... |

#### 4.3.2 Measurement Detail
- แสดงรายละเอียดค่าวัดแต่ละพารามิเตอร์
- กราฟแนวโน้มตามเวลา
- การเปรียบเทียบกับเกณฑ์มาตรฐาน

### 4.4 โมดูลการแจ้งเตือน (Notification Module)

#### 4.4.1 การตั้งค่าการแจ้งเตือน
- ตั้งค่าเกณฑ์สูงสุด/ต่ำสุดสำหรับแต่ละพารามิเตอร์
- เปิด/ปิดการแจ้งเตือนแต่ละประเภท
- ตั้งค่าต่ออุปกรณ์

#### 4.4.2 ประวัติการแจ้งเตือน
- แสดงรายการแจ้งเตือนทั้งหมด
- ทำเครื่องหมายว่าอ่านแล้ว
- กรองตามอุปกรณ์/ประเภท

#### 4.4.3 Push Notifications
- รองรับ Firebase Cloud Messaging (FCM)
- แจ้งเตือนเมื่อค่าเกินเกณฑ์
- รองรับทั้ง Web และ Mobile

### 4.5 โมดูลจัดการผู้ใช้ (User Management Module)
*เฉพาะ Admin/Superadmin*

- แสดงรายการผู้ใช้ทั้งหมด
- เปลี่ยนบทบาทผู้ใช้
- อนุมัติผู้ใช้ใหม่
- รีเซ็ตรหัสผ่าน
- ลบผู้ใช้

### 4.6 โมดูลจัดการอุปกรณ์ (Device Management Module)
*เฉพาะ Admin/Superadmin*

- เพิ่ม/แก้ไข/ลบอุปกรณ์
- ตั้งค่าชื่อแสดง (Display Name)
- ตั้งค่าตำแหน่ง
- ซ่อน/แสดงอุปกรณ์
- เปิด/ปิดการเข้าถึงสาธารณะ (Guest Access)

### 4.7 โมดูลจัดการสิทธิ์เข้าถึงอุปกรณ์ (Device Access Management)
*เฉพาะ Superadmin*

- กำหนดผู้ใช้ที่มีสิทธิ์เข้าถึงแต่ละอุปกรณ์
- จัดการการเข้าถึงแบบกลุ่ม

### 4.8 โมดูลข่าวสาร (News Module)

#### 4.8.1 ดูข่าวสาร (Public)
- แสดงรายการข่าวสาร
- อ่านรายละเอียดข่าว

#### 4.8.2 จัดการข่าวสาร (Admin)
- เพิ่ม/แก้ไข/ลบข่าว
- ตั้งเวลาเผยแพร่
- อัปโหลดรูปภาพ
- เปิด/ปิดการเผยแพร่

### 4.9 โมดูล Moisture Meter
- แสดงข้อมูลเครื่องวัดความชื้น
- รองรับอุปกรณ์ประเภทต่างจากเครื่องวัดคุณภาพข้าว

### 4.10 โมดูล Assistant
- ผู้ช่วยอัจฉริยะ (AI Assistant)
- ช่วยตอบคำถามเกี่ยวกับข้อมูล

---

## 5. โครงสร้างหน้าและเส้นทาง

### 5.1 Public Routes (Guest สามารถเข้าถึงได้)
| เส้นทาง | หน้า | คำอธิบาย |
|--------|------|----------|
| `/` | Index | หน้าแรก/Dashboard |
| `/news` | News | ข่าวสาร |
| `/about-riceflow` | AboutRiceflow | เกี่ยวกับ Riceflow |
| `/equipment` | Equipment | รายการอุปกรณ์ |
| `/device/:deviceCode` | DeviceDetails | รายละเอียดอุปกรณ์ |
| `/device/:deviceCode/:symbol` | MeasurementHistory | ประวัติค่าวัด |
| `/device/:deviceCode/graph-monitor` | DeviceGraphMonitor | กราฟอุปกรณ์ |
| `/device/:deviceCode/graph-summary` | DeviceGraphSummary | สรุปกราฟอุปกรณ์ |
| `/graph-monitor` | GraphMonitor | กราฟรวม |
| `/graph-summary` | GraphSummary | สรุปกราฟรวม |
| `/equipment/moisture-meter` | MoistureMeterEquipment | เครื่องวัดความชื้น |
| `/moisture-meter/:deviceCode` | MoistureMeterDetails | รายละเอียดเครื่องวัดความชื้น |
| `/assistant` | Assistant | ผู้ช่วย AI |
| `/shared/:token` | PublicAnalysisView | ดูผลวิเคราะห์ที่แชร์ |

### 5.2 Protected Routes (ต้องเข้าสู่ระบบ)
| เส้นทาง | หน้า | คำอธิบาย |
|--------|------|----------|
| `/profile` | Profile | โปรไฟล์ผู้ใช้ |
| `/notifications` | NotificationSettings | ตั้งค่าการแจ้งเตือน |
| `/notification-history` | NotificationHistory | ประวัติแจ้งเตือน |
| `/notification-settings` | NotificationSettingsPage | การตั้งค่าแจ้งเตือน |

### 5.3 Admin Routes
| เส้นทาง | หน้า | บทบาทที่ต้องการ |
|--------|------|----------------|
| `/admin` | Admin | admin |
| `/user-management` | UserManagement | admin |
| `/device-management` | DeviceManagement | admin |
| `/news-management` | NewsManagement | admin |
| `/notification-sender` | NotificationSenderPage | admin |
| `/api-test` | APITestPage | admin, superadmin |
| `/device-access-management` | DeviceAccessManagement | superadmin |

### 5.4 Auth Routes
| เส้นทาง | หน้า |
|--------|------|
| `/auth/login` | Login |
| `/logout` | Logout |

---

## 6. ฐานข้อมูล

### 6.1 ตารางหลัก

#### rice_quality_analysis
ตารางหลักสำหรับเก็บข้อมูลการวิเคราะห์คุณภาพข้าว
```sql
- id: number (PK)
- device_code: string
- analyzed_at: timestamp
- head_rice, whole_kernels, total_brokens, ... : number
- thai_datetime: string
- created_at: timestamp
```

#### devices / device_settings
ตารางเก็บข้อมูลอุปกรณ์
```sql
- device_code: string (PK)
- display_name: string
- location: string
- is_active: boolean
- graph_color: string
- report_enabled: boolean
```

#### user_roles
ตารางเก็บบทบาทผู้ใช้
```sql
- id: uuid (PK)
- user_id: uuid (FK -> auth.users)
- role: app_role ('user' | 'admin' | 'superadmin')
```

#### user_device_access
ตารางเก็บสิทธิ์การเข้าถึงอุปกรณ์
```sql
- id: uuid (PK)
- user_id: uuid
- device_code: string
- created_by: uuid
```

#### guest_device_access
ตารางเก็บอุปกรณ์ที่เปิดให้ Guest เข้าถึง
```sql
- id: uuid (PK)
- device_code: string
- enabled: boolean
```

#### notification_settings
ตารางเก็บการตั้งค่าการแจ้งเตือน
```sql
- id: uuid (PK)
- user_id: uuid
- device_code: string
- rice_type_id: string
- enabled: boolean
- min_threshold, max_threshold: number
- min_enabled, max_enabled: boolean
```

#### notifications
ตารางเก็บประวัติการแจ้งเตือน
```sql
- id: uuid (PK)
- user_id: uuid
- device_code: string
- rice_type_id: string
- value: number
- threshold_type: string
- read: boolean
- created_at: timestamp
```

#### news
ตารางเก็บข่าวสาร
```sql
- id: uuid (PK)
- title: string
- content: text
- image_url: string
- publish_date: date
- published: boolean
```

#### user_chart_preferences
ตารางเก็บการตั้งค่ากราฟของผู้ใช้
```sql
- id: uuid (PK)
- user_id: uuid
- device_code: string
- preset_name: string
- selected_metrics: json
```

#### firebase_fcm_tokens
ตารางเก็บ FCM Token สำหรับ Push Notification
```sql
- id: uuid (PK)
- user_id: uuid
- token: string
- device_info: json
- is_active: boolean
```

### 6.2 Views
- `device_data_summary`: สรุปข้อมูลอุปกรณ์
- `guest_enabled_devices`: อุปกรณ์ที่เปิดให้ Guest เข้าถึง

### 6.3 Functions (RPC)
| Function | คำอธิบาย |
|----------|----------|
| `get_devices_with_details` | ดึงรายการอุปกรณ์พร้อมรายละเอียด |
| `get_guest_devices_fast` | ดึงอุปกรณ์สำหรับ Guest (Optimized) |
| `get_user_roles` | ดึงบทบาทของผู้ใช้ |
| `has_role` | ตรวจสอบว่าผู้ใช้มีบทบาทที่กำหนดหรือไม่ |
| `has_device_access` | ตรวจสอบสิทธิ์เข้าถึงอุปกรณ์ |
| `check_notification_thresholds` | ตรวจสอบเกณฑ์แจ้งเตือน |

---

## 7. การแจ้งเตือน

### 7.1 ประเภทการแจ้งเตือน
1. **Threshold Alerts**: แจ้งเตือนเมื่อค่าเกินเกณฑ์ที่กำหนด
2. **System Notifications**: แจ้งเตือนจากระบบ

### 7.2 ช่องทางการแจ้งเตือน
- **In-App Notifications**: แจ้งเตือนภายในแอป
- **Push Notifications**: แจ้งเตือนผ่าน Firebase FCM
- **Sound Alerts**: เสียงแจ้งเตือนภายในแอป

### 7.3 การตั้งค่า
- ตั้งค่าเกณฑ์สูงสุด/ต่ำสุดต่อพารามิเตอร์
- เปิด/ปิดการแจ้งเตือนต่ออุปกรณ์
- เปิด/ปิดเสียงแจ้งเตือน

---

## 8. การรักษาความปลอดภัย

### 8.1 Authentication
- ใช้ Supabase Auth
- รองรับ Email/Password
- Session Management ผ่าน JWT

### 8.2 Authorization
- Role-Based Access Control (RBAC)
- Row Level Security (RLS) บน PostgreSQL
- Protected Routes บน Frontend

### 8.3 Data Security
- HTTPS สำหรับทุก API calls
- Encrypted passwords
- RLS policies สำหรับทุกตาราง

---

## 9. ข้อกำหนดที่ไม่ใช่ฟังก์ชัน

### 9.1 Performance
- หน้าโหลดภายใน 3 วินาที
- Real-time data update ภายใน 1 วินาที
- รองรับผู้ใช้พร้อมกัน 100+ คน

### 9.2 Availability
- Uptime 99.9%
- Graceful error handling

### 9.3 Scalability
- รองรับการเพิ่มอุปกรณ์ได้ไม่จำกัด
- Database indexing ที่เหมาะสม

### 9.4 Compatibility
- **Browsers**: Chrome, Safari, Firefox, Edge (2 เวอร์ชันล่าสุด)
- **Mobile**: iOS 13+, Android 8+
- **Responsive**: 320px - 2560px

### 9.5 Localization
- รองรับภาษาไทยและภาษาอังกฤษ
- Timezone: Asia/Bangkok

---

## 📝 Change Log

| Version | Date | Changes |
|---------|------|---------|
| 2.0 | 2025-11-30 | เอกสารฉบับสมบูรณ์ |
| 1.0 | - | เวอร์ชันแรก |

---

## 📞 ติดต่อ

- **Project**: Riceflow
- **Website**: https://setup.riceflow.app
- **Documentation**: /docs/REQUIREMENTS.md
